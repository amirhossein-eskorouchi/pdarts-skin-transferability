# Fictional manifest example

`image_manifest.example.csv` demonstrates the required column structure.

Every row is invented. The sample IDs, patient IDs, paths, and labels are not
taken from ISIC 2019, PAD-UFES-20, DermaMNIST, or any research participant.

The example exists only for interface validation and documentation.
