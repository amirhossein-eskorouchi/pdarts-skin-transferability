# Reproduction outputs

This directory is reserved for future outputs generated by maintained,
documented workflows.

No reproduction result is currently committed.

Publication values belong only in `results/publication_record/`.

Large outputs, predictions, checkpoints, NumPy arrays, and dataset-derived
artifacts remain excluded from Git. Small canonical summaries may be committed
only after their provenance and generation procedure are documented.
